# instructions to work on the environment: linux / bash

- my os is **linux ubuntu 24**, my shell is **bash**
- use the command **sudo apt get install** to install new shell commands
- use the command **pyhton** to run python scripts and not **python3**
